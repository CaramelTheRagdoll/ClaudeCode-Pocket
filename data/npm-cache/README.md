# data/npm-cache/

npm package cache directory.

Created automatically by `claude.cmd` if missing. Speeds up `npm install` by caching downloaded packages.

> Cache is portable-mode only. In system/hybrid mode, npm uses its default cache location.
