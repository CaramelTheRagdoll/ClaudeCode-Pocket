# data/home/

User home directory — `HOME` is redirected here at startup.

Created automatically by `claude.cmd` if missing. Contains after first run:
- `.claude/` — Claude Code credentials, settings, session history
- `.cc-switch/` — CC Switch provider configs and usage data
- `.gitconfig` — Git user config (if configured)

> Contains OAuth tokens. Never commit or share.
