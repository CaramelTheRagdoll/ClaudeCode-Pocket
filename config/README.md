# config/

npm configuration directory.

`claude.cmd` sets `NPM_CONFIG_USERCONFIG` to `config/npmrc` in portable and hybrid modes. Contains npm registry and cache settings scoped to the portable edition.

> In system mode, npm uses its global configuration instead.
