# downloads/

Temporary download storage for `build.cmd`.

Node.js zip archives are downloaded here during the build process. This directory is cleaned up on successful builds and excluded from Git.

> Safe to delete manually if a build was interrupted.
