# Build artifacts — temporary download files (entire directory)
downloads/

# CC Switch binaries — large, downloaded separately from GitHub Releases
cc-switch/CC Switch.exe
cc-switch/*.exe
cc-switch/*.dll
