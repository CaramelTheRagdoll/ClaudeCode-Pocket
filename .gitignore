# Runtime binaries — downloaded by build.cmd
runtime/

# Build artifacts
downloads/

# User data — contains credentials, tokens, and session info
data/

# CC Switch binaries — large, downloaded separately from GitHub Releases
cc-switch/CC Switch.exe
cc-switch/*.exe
cc-switch/*.dll
